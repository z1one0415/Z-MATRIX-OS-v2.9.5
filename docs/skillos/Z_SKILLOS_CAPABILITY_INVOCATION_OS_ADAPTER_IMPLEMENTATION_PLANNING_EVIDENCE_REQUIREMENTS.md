# Adapter Evidence Requirements
## Status: Z_SKILLOS_CAPABILITY_INVOCATION_OS_ADAPTER_EVIDENCE_REQUIREMENTS_READY
Adapter evidence requirements: source snapshot (pre-state hash), request hash (input payload hash), output hash (output payload hash), intermediate evidence (per-step hashes), citation/file evidence links, no deletion policy (append-only, immutable), rollback evidence (pre-state for rollback), no evidence writes until separately authorized.
